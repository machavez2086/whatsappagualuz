<nav class="navbar navbar-expand-lg bg-primary fixed-top navbar-transparent " color-on-scroll="400">
    <div class="container">
        <div class="navbar-translate">
            <button class="navbar-toggler navbar-toggler pull-right" type="button" data-toggle="collapse"
                    data-target="#navigation" aria-controls="navigation-index" aria-expanded="false"
                    aria-label="Toggle navigation">
                <span class="navbar-toggler-bar bar1"></span>
                <span class="navbar-toggler-bar bar2"></span>
                <span class="navbar-toggler-bar bar3"></span>
            </button>
        </div>
        <div class="collapse navbar-collapse justify-content-end">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link js-scroll-trigger" href="{{ path('home') }}#services" title="Enviar mensaje">¿Tiene un problema?</a>
                </li>
                {% if app.user%}
                    <li class="nav-item nav-link color-white">
                        {{app.user.username}}
                    </li>
                {% endif %}
                {% if not app.user%}
                    <li class="nav-item color-white">
                        <a class="nav-link" href="{{ path('fos_user_security_login') }}" title="Autenticación"><i class="fa fa-user"></i></a>
                    </li>
                {% else %}
                    <li class="nav-item dropdown color-white">
                        <i href="#pablo" class="fa fa-user-circle nav-link dropdown-toggle" id="navbarDropdown"
                           data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"></i>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{path('sonata_admin_dashboard')}}">Administración</a>
{#                            <a class="dropdown-item" href="{{path('dashboard')}}">Dashboard</a>#}
                            <a class="dropdown-item" href="{{ path('fos_user_profile_edit') }}">Editar perfil</a>
                            <a class="dropdown-item" href="{{ path('sonata_user_change_password') }}">Cambiar contraseña</a>
                            
                            <a class="dropdown-item" href="{{ path('fos_user_security_logout') }}">Salir</a>
                        </div>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>