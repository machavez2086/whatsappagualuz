{#

This file is part of the Sonata package.

(c) Thomas Rabaix <thomas.rabaix@sonata-project.org>

For the full copyright and license information, please view the LICENSE
file that was distributed with this source code.

#}

{% extends base_template %}

{% block title %}
    {% if admin.id(object) is not null %}
        {{ "title_edit"|trans({'%name%': admin.toString(object)|truncate(15) }, 'SonataAdminBundle') }}
    {% else %}
        {{ "title_create"|trans({}, 'SonataAdminBundle') }}
    {% endif %}
{% endblock %}

{% block navbar_title %}
    {{ block('title') }}
{% endblock %}

{%- block actions -%}
    {% include 'SonataAdminBundle:CRUD:action_buttons.html.twig' %}
{%- endblock -%}

{% block tab_menu %}{{ knp_menu_render(admin.sidemenu(action), {'currentClass' : 'active', 'template': sonata_admin.adminPool.getTemplate('tab_menu_template')}, 'twig') }}{% endblock %}

{% use 'WhatsappBundle:CRUD:base_edit_form.html.twig' with form as parentForm %}
{% import "SonataAdminBundle:CRUD:base_edit_form_macro.html.twig" as form_helper %}

{% block form %}
    {{ block('parentForm') }}
    
    
    <script type="text/javascript">
     $(document).ready(function()
       {        
        if($("#{{ admin.uniqId }}_configuration").val() !=""){
           var configurationId=$("#{{ admin.uniqId }}_configuration").val();
           var dataString = {configurationId: configurationId, messageId: "{{object.id}}"};
           $.ajax
           ({
               type: "get",
               url: "{{ admin.generateObjectUrl('getGroupsByConfiguration', object) }}",
               data: dataString,
               cache: false,
               success: function(html)
               {
                   $("#{{ admin.uniqId }}_whatsappGroup").select2('val','');
                   $("#{{ admin.uniqId }}_whatsappGroup").html(html.html);
                    $("#{{ admin.uniqId }}_whatsappGroup").select2('val',html.value);
                    $("#{{ admin.uniqId }}_whatsappGroup").trigger('change');
                    changeGroup();
               } 
           });
        }
        
         $("#{{ admin.uniqId }}_configuration").change(function()
        {
{#             alert("asf");#}
        var configurationId=$("#{{ admin.uniqId }}_configuration").val();
{#        var dataString = 'id='+ id+'&departamentId='+ {{object.id}};#}
        var dataString = {configurationId: configurationId, messageId: "{{object.id}}"};
{#        alert(dataString);#}
        $.ajax
        ({
            type: "get",
            url: "{{ admin.generateObjectUrl('getGroupsByConfiguration', object) }}",
            data: dataString,
            cache: false,
            success: function(html)
            {
{#                var items = document.getElementsByClassName("item");#}
                
{#                console.log(html.length);#}
                $("#{{ admin.uniqId }}_whatsappGroup").select2('val','');
                $("#{{ admin.uniqId }}_whatsappGroup").html(html.html);
                $("#{{ admin.uniqId }}_whatsappGroup").select2('val',html.value);
                changeGroup();
            } 
        });
       });

     });
</script>
{% endblock %}
